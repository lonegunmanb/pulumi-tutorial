# 准备 Automation API

安装 Automation API 依赖已经由 `@pulumi/pulumi` 提供。后续正式章节会新增 `automation.ts`，使用 `LocalWorkspace` 和 `Stack` API 执行预览与部署。

你可以先查看版本：

```bash
npm list @pulumi/pulumi
pulumi version
```{{exec}}