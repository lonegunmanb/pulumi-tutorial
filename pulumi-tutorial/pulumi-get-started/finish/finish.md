# 完成

你已经在 Linux 上从零安装了 Pulumi：用安装脚本部署 CLI、把 `~/.pulumi/bin` 加入 `PATH`、用 `pulumi version` 验证，并用 `pulumi login --local` 选择了本地后端。

下一步可以回到正文学习 Engine、Language Host 与 Provider Plugin 的协作机制，并写下你的第一个 Pulumi 程序。