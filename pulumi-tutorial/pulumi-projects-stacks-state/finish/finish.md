# 完成

你已经完成 AWS / MiniStack 版实验：

- 用一个 `aws-infra` Project 创建 `dev` 与 `prod` 两个 Stack。
- 为不同 Stack 设置独立 Config 与 Secret。
- 通过 Stack Outputs 读取模拟 S3 Bucket 名称。
- 导出本地 State 并观察 URN、资源类型和 Outputs。
- 用 `aws-consumer` Project 通过 `StackReference` 读取上游输出。

下一步可以回到正文，切换到 Azure / miniblue 版实验，观察同一组 Pulumi 概念如何迁移到另一个云模型。