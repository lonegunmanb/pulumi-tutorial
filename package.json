{
  "name": "pulumi-tutorial",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs --host 0.0.0.0",
    "sync-sidebar": "node scripts/sync-sidebar.mjs",
    "sync-killercoda": "node scripts/sync-killercoda-structure.mjs && node scripts/sync-setup-common.mjs",
    "prebuild": "npm run sync-sidebar && npm run sync-killercoda",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --host 0.0.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}